# ---- Server / API ----
fastapi>=0.121.3
uvicorn[standard]>=0.38.0
gunicorn>=23.0.0

# ---- Core scientific libs ----
numpy>=2.0.0,<2.4.0
Pillow>=10.0.1
opencv-python>=4.8.1
matplotlib>=3.8.2
tqdm>=4.66.1

# ---- Machine Learning (CPU only) ----
# PyTorch CPU packages must be installed via PyTorch index:
# pip install torch==2.2.2+cpu torchvision==0.17.2+cpu torchaudio==2.2.2+cpu --index-url https://download.pytorch.org/whl/cpu
torch==2.4.0+cpu
torchvision==0.19.0+cpu
torchaudio==2.4.0+cpu

# ---- CLIP and image models ----
open-clip-torch>=2.23.0
timm>=0.9.12

# ---- Vector search ----
faiss-cpu>=1.13.0

# ---- Object detection ----
ultralytics>=8.0.0
inference-sdk>=0.9.0       # Roboflow hosted inference (set ROBOFLOW_API_KEY + ROBOFLOW_MODEL_ID)

# ---- Web / Utils ----
python-multipart>=0.0.6
jinja2>=3.1.2
